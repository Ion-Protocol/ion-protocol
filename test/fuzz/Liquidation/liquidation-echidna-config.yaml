# Fuzzes the assert statements inside the Liquidation contract 

workers: 5

testLimit: 10000

# solcArgs: "--optimize --optimize-runs 0"

cryticArgs: [
    "--solc-remaps",
    "@openzeppelin/contracts/=lib/openzeppelin-contracts/contracts/
    @openzeppelin/contracts-upgradeable/=lib/openzeppelin-contracts-upgradeable/contracts/
    ds-test/=lib/forge-std/lib/ds-test/src/
    forge-std/=lib/forge-std/src/
    openzeppelin-contracts/=lib/openzeppelin-contracts/
    openzeppelin/=lib/openzeppelin-contracts/contracts/"
  ]

# prefix: "assert_"

corpusDir: "test/fuzz/Liquidation/liquidation-echidna-corpus"

testMode: assertion 

allContracts: true
filterBlacklist: false # if false, allows filterFunctions; if true, disallow filterFunctions
filterFunctions: [
  # "LiquidationEchidna.test_assert_false()", 
  # "LiquidationEchidna.changeExchangeRate(uint256)",
  "LiquidationEchidna.borrow(uint256,uint256)"
  # "LiquidationEchidna.supply(uint256)", 
  # "LiquidationEchidna.liquidate()"
  ]
